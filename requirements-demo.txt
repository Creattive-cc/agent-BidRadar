# Dependências MÍNIMAS da demo Streamlit do BidRadar.
# Reusa o código real em agent/ sem precisar das libs pesadas de GCP/LLM,
# mantendo a imagem leve e o build rápido no Cloud Run.
streamlit>=1.40
pandas>=2.2
requests>=2.32
pydantic>=2.8
sqlalchemy>=2.0
python-dotenv>=1.0.1
# 'playwright' (apenas o pacote, SEM navegadores) é necessário porque
# agent/scraper/__init__.py importa o scraper ConLicitação no topo.
# A demo NÃO usa Playwright — os scrapers de navegador ficam desabilitados.
playwright>=1.59
google-genai>=1.0
google-auth>=2.29
